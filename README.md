# Description:

A simple web Todo List allowing adding, editing, deleting, and marking tasks as complete, with text length limit and live counter, built with JavaScript and DOM, using Boxicons for icons and HTML/CSS for the interface, ready for customization and extension.
